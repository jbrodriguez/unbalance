import React from 'react';

export const Description: React.FunctionComponent = () => {
  return (
    <span>
      Verify the planned new free disk space of your drives, if you're ok, click
      on next, otherwise click on prev
    </span>
  );
};
