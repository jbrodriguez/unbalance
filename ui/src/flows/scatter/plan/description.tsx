import React from 'react';

export const Description: React.FunctionComponent = () => {
  return (
    <span>
      Monitor the progress of the scatter planning operation. It will show you
      of any folders/files that can't be transferred due to size constraints (if
      any).
    </span>
  );
};
