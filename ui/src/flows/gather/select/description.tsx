import React from 'react';

export const Description: React.FunctionComponent = () => {
  return (
    <span>
      Select <span className="font-bold">ONE or MORE</span> source folder(s),
      then click next
    </span>
  );
};
