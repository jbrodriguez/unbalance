import React from 'react';

export const Description: React.FunctionComponent = () => {
  return (
    <span>
      Select <span className="font-bold">ONE or MORE</span> target disk(s), the
      folder(s)/file(s) will be moved to this disk(s)
    </span>
  );
};
